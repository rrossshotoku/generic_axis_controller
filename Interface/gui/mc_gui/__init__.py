"""mc_gui - PC tool for the CMC object dictionary (UDP).

Speaks the OD-over-UDP contract in ../NETWORK_UDP_SPEC.md, with the OD layout and
scaling parsed at runtime from ../mc_if_od.h.
"""
__version__ = "1.0.0"
